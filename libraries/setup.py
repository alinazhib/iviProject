from setuptools import setup

setup(
    name='robotframework-testlibraries',
    version='0.2',
    description='robotframework-testlibraries is a set of libraries for Robot Framework',
    packages=['TestLibrary'],
    zip_safe=False
)
